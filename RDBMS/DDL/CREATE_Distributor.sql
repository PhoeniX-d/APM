CREATE TABLE Distributor(
    Distributor_ID VARCHAR(10),
    Distributor_Name VARCHAR(20),
    Address VARCHAR(100),
    Mobilenumber NUMBER(10),
    Email VARCHAR(30),
    CONSTRAINT PK PRIMARY KEY(Distributor_ID)
);